namespace Challenges;

public class NumberOfAtoms
{
    // the return value is supposed to be a string
    // the formatting of that can be figured out later
    public static Dictionary<char, int> Soln(string formula)
    {
        Dictionary<char, int> map = [];

        // some ideas - maybe a stack approach
        // also if parentheses encountered, maybe use recursion

        return map;
    }
}